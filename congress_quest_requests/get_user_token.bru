meta {
  name: get_user_token
  type: http
  seq: 2
}

post {
  url: {{base_url}}/auth/v1/token?grant_type=password
  body: json
  auth: inherit
}

params:query {
  grant_type: password
}

body:json {
  {
    "email": "{{email}}",
    "password": "{{password}}"
  }
}
